const express = require("express")

express = require("express")
const app = express()
const pot = 3000

app.get('/', (req,res =>
	res.send("test");
})

	app.listen(port, () => {
	console.log(`{4 kittne)
	)
